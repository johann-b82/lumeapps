[pytest]
asyncio_mode = auto
testpaths = tests
markers =
    integration: end-to-end tests that require real Directus, Postgres LISTEN/NOTIFY, or host docker. Skipped by default; run with `pytest -m integration`.
    slow: slow tests; mark to deprioritise in default runs.
addopts = -m "not integration"
